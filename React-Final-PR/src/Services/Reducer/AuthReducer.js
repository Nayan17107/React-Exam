const initialState = {
    user: null,
    isLoading: false,
    errorMsg: ""
};

const authReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default authReducer;